Just a default babel / webpack / node-sass / eslint / prettier template for "pure" JS tools (no react or other frameworks)

ToDo:

- add typescript branch
- change placement of js inject so example page works, remove jquery
